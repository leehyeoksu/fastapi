# 📖 용어집 (Glossary)

**모든 용어를 초보자 눈높이로 설명합니다**

---

## 🗄️ 데이터베이스 관련

### 데이터베이스 (Database)
**뜻**: 데이터를 저장하는 창고

**비유**: 도서관
- 책(데이터)을 체계적으로 보관
- 필요할 때 빠르게 찾을 수 있음
- 여러 사람이 동시에 이용 가능

**예시**:
```python
# 데이터베이스 = 유저 정보를 저장하는 곳
users = [
    {"id": 1, "name": "홍길동"},
    {"id": 2, "name": "김철수"}
]
```

**종류**:
- **RDBMS** (관계형): MySQL, PostgreSQL - 엑셀처럼 표 형태
- **NoSQL**: MongoDB - JSON 형태로 저장

🔗 [DB 기초 - 생활코딩](https://opentutorials.org/course/3885)

---

### 쿼리 (Query)
**뜻**: 데이터베이스에게 하는 "질문" 또는 "명령"

**비유**: 도서관 사서에게 하는 요청
- "컴퓨터 관련 책 찾아주세요" = SELECT
- "이 책 반납합니다" = INSERT
- "책 정보 수정해주세요" = UPDATE

**예시**:
```sql
-- SQL 쿼리 (데이터베이스 언어)
SELECT * FROM users WHERE age >= 18;
```

```python
# ORM 쿼리 (Python으로 쿼리)
users = db.query(User).filter(User.age >= 18).all()
```

**쿼리 종류**:
- `SELECT`: 조회 (읽기)
- `INSERT`: 삽입 (생성)
- `UPDATE`: 수정
- `DELETE`: 삭제

🔗 [SQL 쿼리 배우기](https://www.w3schools.com/sql/)

---

### 세션 (Session)
**뜻**: 데이터베이스와의 "대화 시간"

**비유**: 은행 업무
1. 창구에 감 (세션 열기)
2. 업무 처리 (쿼리 실행)
3. 창구 떠남 (세션 닫기)

**왜 중요?**: 
- DB 연결은 한정적 (동시에 1000명만 접속 가능 등)
- 세션을 안 닫으면 연결이 고갈됨
- 서버가 멈출 수 있음!

**예시**:
```python
# 세션 시작
session = SessionLocal()

# DB 작업
user = session.query(User).first()

# 세션 종료 (필수!)
session.close()

# 또는 자동으로 닫기
with SessionLocal() as session:
    user = session.query(User).first()
# with 블록 끝나면 자동으로 close()
```

🔗 [SQLAlchemy Session](https://docs.sqlalchemy.org/en/20/orm/session.html)

---

### 마이그레이션 (Migration)
**뜻**: 데이터베이스 구조 변경 기록

**비유**: Git for Database
- Git = 코드 변경 기록
- Migration = DB 구조 변경 기록

**왜 필요?**:
- DB에 컬럼 추가/삭제 시 기록 필요
- 팀원들과 DB 구조 동기화
- 배포 시 자동으로 DB 구조 업데이트

**예시**:
```bash
# 1. DB 구조 변경 (컬럼 추가)
class User(Base):
    id = Column(Integer, primary_key=True)
    name = Column(String(50))
    email = Column(String(100))  # 새로 추가!

# 2. 마이그레이션 파일 생성
alembic revision --autogenerate -m "Add email column"

# 3. DB에 적용
alembic upgrade head

# 4. 되돌리기
alembic downgrade -1
```

**마이그레이션 도구**:
- **Alembic**: SQLAlchemy용
- **Django Migrations**: Django용
- **Flyway**: Java용

🔗 [Alembic 튜토리얼](https://alembic.sqlalchemy.org/en/latest/tutorial.html)

---

### 트랜잭션 (Transaction)
**뜻**: 여러 작업을 하나로 묶는 것

**비유**: 은행 이체
- A 계좌에서 출금
- B 계좌에 입금
- 둘 다 성공 or 둘 다 실패

**ACID 원칙**:
- **A**tomicity (원자성): 전부 성공 or 전부 실패
- **C**onsistency (일관성): 규칙 위반 불가
- **I**solation (고립성): 동시 실행 시 간섭 없음
- **D**urability (지속성): 저장하면 영구 보존

**예시**:
```python
try:
    session.add(withdraw(user_a, 100))
    session.add(deposit(user_b, 100))
    session.commit()  # 둘 다 성공
except:
    session.rollback()  # 하나라도 실패하면 취소
```

🔗 [트랜잭션 이해](https://www.mongodb.com/basics/acid-transactions)

---

### ORM (Object-Relational Mapping)
**뜻**: DB 테이블을 Python 클래스로 다루기

**비유**: 번역기
- SQL 언어를 Python으로 번역
- Python만 알아도 DB 조작 가능

**장점**:
- SQL 몰라도 됨
- 타입 안전
- 코드 재사용

**예시**:
```python
# SQL로 작성
SELECT * FROM users WHERE age >= 18;

# ORM으로 작성 (Python)
users = db.query(User).filter(User.age >= 18).all()
```

🔗 [ORM이란?](https://www.freecodecamp.org/news/what-is-an-orm-the-meaning-of-object-relational-mapping-database-tools/)

---

## 🌐 웹 개발 관련

### API (Application Programming Interface)
**뜻**: 프로그램끼리 대화하는 규칙

**비유**: 식당 메뉴판
- 손님(프론트엔드)이 메뉴판(API 문서) 보고 주문
- 주방(백엔드)에서 요리 제공

**예시**:
```python
# API 엔드포인트
@app.get("/api/users")  # 메뉴판의 메뉴
def get_users():
    return [{"id": 1, "name": "홍길동"}]

# 클라이언트가 호출
GET http://localhost:8000/api/users
```

🔗 [API 기초](https://www.ibm.com/topics/api)

---

### REST (REpresentational State Transfer)
**뜻**: API 설계 방식

**원칙**:
- URL은 명사 (동사 X)
- HTTP 메서드로 동작 표현

**예시**:
```python
# ✅ RESTful
GET    /api/users      # 조회
POST   /api/users      # 생성
PUT    /api/users/1    # 수정
DELETE /api/users/1    # 삭제

# ❌ Non-RESTful
GET /api/getUsers
POST /api/createUser
POST /api/updateUser
POST /api/deleteUser
```

🔗 [REST API 가이드](https://restfulapi.net/)

---

### JSON (JavaScript Object Notation)
**뜻**: 데이터를 주고받는 형식

**비유**: 택배 상자
- 데이터를 규격화된 상자에 담아 전송

**예시**:
```json
{
  "id": 1,
  "name": "홍길동",
  "email": "hong@example.com"
}
```

```python
# Python에서 JSON
import json

data = {"id": 1, "name": "홍길동"}
json_str = json.dumps(data)  # Python → JSON
data = json.loads(json_str)  # JSON → Python
```

🔗 [JSON 배우기](https://www.json.org/)

---

### HTTP (HyperText Transfer Protocol)
**뜻**: 웹에서 데이터를 주고받는 규칙

**주요 메서드**:
- `GET`: 조회
- `POST`: 생성
- `PUT`: 전체 수정
- `PATCH`: 일부 수정
- `DELETE`: 삭제

**상태 코드**:
- `200 OK`: 성공
- `201 Created`: 생성 성공
- `400 Bad Request`: 잘못된 요청
- `401 Unauthorized`: 인증 필요
- `404 Not Found`: 없음
- `500 Internal Server Error`: 서버 에러

🔗 [HTTP 완벽 가이드](https://developer.mozilla.org/ko/docs/Web/HTTP)

---

## 🔧 개발 도구

### Git
**뜻**: 코드 버전 관리 도구

**비유**: 타임머신
- 과거 코드로 되돌리기 가능
- 여러 사람이 동시에 작업 가능

**기본 명령어**:
```bash
git init          # 저장소 초기화
git add .         # 변경사항 스테이징
git commit -m ""  # 저장
git push          # 서버에 업로드
git pull          # 서버에서 다운로드
```

🔗 [Git 입문](https://git-scm.com/book/ko/v2)

---

### Docker
**뜻**: 앱을 컨테이너로 포장

**비유**: 택배 상자
- 앱과 필요한 모든 것을 하나로 포장
- 어디서든 똑같이 실행

**예시**:
```dockerfile
FROM python:3.12
COPY . /app
CMD ["python", "app.py"]
```

🔗 [Docker 시작하기](https://docs.docker.com/get-started/)

---

### CI/CD
**뜻**: 자동화된 테스트 & 배포

**CI (Continuous Integration)**:
- 코드 푸시 → 자동 테스트

**CD (Continuous Deployment)**:
- 테스트 통과 → 자동 배포

🔗 [CI/CD 가이드](https://www.redhat.com/ko/topics/devops/what-is-ci-cd)

---

## 📚 Python 관련

### 비동기 (Async/Await)
**뜻**: 기다리는 동안 다른 일 하기

**비유**:
- 동기: 커피 다 내릴 때까지 멍때리기
- 비동기: 커피 내리는 동안 청소하기

**예시**:
```python
# 동기 (5초 + 3초 = 8초)
def make_coffee(): sleep(5)
def make_tea(): sleep(3)

# 비동기 (동시 실행 = 5초)
async def make_coffee(): await sleep(5)
async def make_tea(): await sleep(3)
```

🔗 [Async IO 완벽 가이드](https://realpython.com/async-io-python/)

---

### 타입 힌트 (Type Hints)
**뜻**: 변수/함수 타입 명시

**예시**:
```python
# 타입 힌트 없이
def add(a, b):
    return a + b

# 타입 힌트와 함께
def add(a: int, b: int) -> int:
    return a + b
```

**장점**:
- IDE 자동완성
- 버그 사전 발견
- 코드 가독성

🔗 [타입 힌트 가이드](https://docs.python.org/3/library/typing.html)

---

## 📖 추천 학습 자료

### 🎥 유튜브
- [생활코딩 - DB 기초](https://www.youtube.com/@생활코딩)
- [코딩애플 - Python](https://www.youtube.com/@codingapple)
- [노마드 코더](https://www.youtube.com/@nomadcoders)

### 📚 블로그
- [Real Python](https://realpython.com/)
- [44BITS - DevOps](https://www.44bits.io/ko)
- [Naver D2](https://d2.naver.com/)

### 💻 인터랙티브
- [SQLBolt](https://sqlbolt.com/) - SQL 연습
- [Python Tutor](https://pythontutor.com/) - 코드 시각화
- [Regex101](https://regex101.com/) - 정규식 연습

### 📖 공식 문서
- [FastAPI](https://fastapi.tiangolo.com/ko/)
- [SQLAlchemy](https://docs.sqlalchemy.org/)
- [Pydantic](https://docs.pydantic.dev/)
- [pytest](https://docs.pytest.org/)

### 🌐 커뮤니티
- [Stack Overflow](https://stackoverflow.com/)
- [Reddit r/Python](https://www.reddit.com/r/Python/)
- [Reddit r/FastAPI](https://www.reddit.com/r/FastAPI/)

---

**이 용어집을 북마크하고, 모르는 용어가 나올 때마다 찾아보세요!** 📖
