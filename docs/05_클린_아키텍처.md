# 📘 05_클린_아키텍처

**학습 목표**: 유지보수 쉬운 코드 작성

---

## 🏛️ MVC vs 3-Tier vs Clean Architecture

### MVC (Model-View-Controller)
```
Controller → Model
    ↓         ↓
  View ← ← ← ←
```

### 3-Tier Architecture
```
Presentation (Controller)
     ↓
Business Logic (Service)
     ↓
Data Access (Repository)
```

### Clean Architecture
```
     Entities (핵심 비즈니스)
          ↓
    Use Cases (비즈니스 로직)
          ↓
  Interface Adapters (Controller, Presenter)
          ↓
  Frameworks (FastAPI, SQLAlchemy)
```

---

## 🎯 SOLID 원칙

### 1. Single Responsibility (단일 책임)
```python
# ❌ 나쁜 예 - 너무 많은 책임
class UserService:
    def create_user(self): ...
    def send_email(self): ...  # 이메일은 별도 서비스로!
    def log_action(self): ...  # 로깅도 별도로!

# ✅ 좋은 예
class UserService:
    def create_user(self): ...

class EmailService:
    def send_email(self): ...

class Logger:
    def log(self): ...
```

### 2. Open/Closed (개방/폐쇄)
```python
# 확장에는 열려있고, 수정에는 닫혀있어야 함

# ❌ 나쁜 예
def calculate_area(shape):
    if shape.type == "circle":
        return 3.14 * shape.radius ** 2
    elif shape.type == "rectangle":  # 새로운 도형 추가 시 수정 필요!
        return shape.width * shape.height

# ✅ 좋은 예
class Shape(ABC):
    @abstractmethod
    def area(self): pass

class Circle(Shape):
    def area(self):
        return 3.14 * self.radius ** 2

class Rectangle(Shape):
    def area(self):
        return self.width * self.height
```

---

## 💻 실전 적용

### Repository Pattern
```python
class UserRepository:
    def __init__(self, db: Session):
        self.db = db
    
    def save(self, user: User):
        self.db.add(user)
        self.db.commit()
```

### Service Layer
```python
class UserService:
    def __init__(self, repo: UserRepository):
        self.repo = repo
    
    def register(self, data):
        # 비즈니스 로직
        user = User(**data)
        return self.repo.save(user)
```

---

**다음 학습**: [06_테스트_완벽_가이드.md](./06_테스트_완벽_가이드.md) 🚀
